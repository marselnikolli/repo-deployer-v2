- card/grid view
- more intelligent categorization system (miss categorized)
- bulk operational buttons should stay always visible
- smart deploy still not working (should be expanded the logic how this will work in order to be isolated as much as possible)
- how to deal with 1000+ reps (management and smart access)
- expand browser-extention to retrieve all metadata upon import directly from the opened page (when its only 1 browser window open, when multiple URLs being imported use a stealth browser to get metadata)
- check the last date of repo update (automate this somehow for further workflow: if a repo has more than 1year that is not updated, there should be a status or a separate page to show these in order to cleanup)
- implement: https://github.com/tg12/phantomstars for proper check/