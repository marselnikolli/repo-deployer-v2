# GitHub API Configuration
# Get your personal access token at: https://github.com/settings/tokens
# 
# Instructions:
# 1. Go to https://github.com/settings/tokens
# 2. Click "Generate new token (classic)"
# 3. Give it a name like "repo-deployer"
# 4. Select scopes: check only "public_repo" (read-only access to public repositories)
# 5. Click "Generate token"
# 6. Copy the token (starts with "ghp_")
# 7. Paste it below
#
# Scopes needed: public_repo (read-only access to public repositories)
# 
# Optional: Leave empty to use unauthenticated mode (60 req/hour limit)
# With token: 5000 req/hour (100x higher, recommended for 4K+ repositories)
#
# ⚠️  SECURITY: Never commit the .env file to git!
#     The .env file is in .gitignore and should only exist on your machine.
#
GITHUB_TOKEN=your_github_token_here

# === Core Configuration ===
SECRET_KEY=your-secret-key-change-in-production-min-32-chars
DEBUG=false
LOG_LEVEL=INFO

# === Database ===
# This is configured automatically in docker-compose.yml
# DATABASE_URL=postgresql://postgres:postgres@db:5432/repo_deployer

# === Redis ===
# This is configured automatically in docker-compose.yml
# REDIS_URL=redis://redis:6379/0

# === GitHub OAuth (for Login with GitHub) ===
# Get OAuth credentials from: https://github.com/settings/developers
# Steps:
# 1. Go to GitHub Settings > Developer settings > OAuth Apps
# 2. Click "New OAuth App"
# 3. Fill in the form:
#    - Application name: Repo Deployer
#    - Homepage URL: http://localhost:3000
#    - Authorization callback URL: http://localhost:3000/auth/github/callback
# 4. Copy the Client ID and generate a Client secret
#
GITHUB_OAUTH_CLIENT_ID=your_github_oauth_client_id_here
GITHUB_OAUTH_CLIENT_SECRET=your_github_oauth_client_secret_here
GITHUB_OAUTH_REDIRECT_URI=http://localhost:3000/auth/github/callback

# === Google OAuth (for Login with Google) ===
# Get OAuth credentials from: https://console.cloud.google.com/
# Steps:
# 1. Create a new Google Cloud project
# 2. Enable Google+ API
# 3. Go to Credentials > Create OAuth 2.0 Client ID
# 4. Select "Web application"
# 5. Add authorized JavaScript origins: http://localhost:3000
# 6. Add authorized redirect URIs: http://localhost:3000/auth/google/callback
# 7. Copy the Client ID and Client secret
#
GOOGLE_OAUTH_CLIENT_ID=your_google_oauth_client_id_here
GOOGLE_OAUTH_CLIENT_SECRET=your_google_oauth_client_secret_here
GOOGLE_OAUTH_REDIRECT_URI=http://localhost:3000/auth/google/callback

# === Email Configuration (for password reset & notifications) ===
# Currently using Gmail SMTP - update these values for other providers
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASSWORD=your_app_password_here
SMTP_FROM_EMAIL=noreply@repo-deployer.local
SMTP_FROM_NAME=Repo Deployer

# === Application URLs ===
# These are for reference and internal redirects
BACKEND_URL=http://localhost:8000
FRONTEND_URL=http://localhost:3000

# === Deployment ===
# These are configured in docker-compose.yml
# DOCKER_SOCKET=/var/run/docker.sock
# MIRROR_DIR=/app/mirrors
